{
  "name": "nodejs-html",
  "version": "1.0.0",
  "description": "## deploy * Copy `sample.env` to `.env` * Fil in the values of `.env` * Run: ```bash serverless deploy ```",
  "main": "handler.js",
  "scripts": {
    "deploy": "serverless deploy",
    "start": "serverless offline start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serve": "^13.0.2",
    "serverless": "^3.3.0",
    "serverless-offline": "^8.5.0"
  }
}
